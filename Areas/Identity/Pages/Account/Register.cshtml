@page
@model RegisterModel
@{
    ViewData["Title"] = "Inscription";
    Layout = null;
}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Inscription - Mondial Pronos</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="auth-body">
    <div class="auth-split">
        <aside class="auth-aside">
            <a href="/" class="auth-brand">
                <span class="auth-brand-mark">MP</span>
                <span>Mondial Pronos</span>
            </a>
            <div class="auth-aside-content">
                <h1>Entre dans le jeu.</h1>
                <p>Crée ton compte, pronostique les matchs de la Coupe du Monde et défie les autres joueurs.</p>
                <ul class="auth-points">
                    <li><span>3</span> points pour un score exact</li>
                    <li><span>1</span> point pour le bon résultat</li>
                    <li><span>★</span> classement mis à jour en direct</li>
                </ul>
            </div>
            <div class="auth-aside-foot">Coupe du Monde 2026</div>
        </aside>

        <main class="auth-main">
            <div class="auth-form-box">
                <h2 class="auth-h2">Créer un compte</h2>
                <p class="auth-sub">Déjà inscrit ? <a asp-page="./Login">Se connecter</a></p>

                <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

                    <div class="auth-field">
                        <label asp-for="Input.Email">Adresse e-mail</label>
                        <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="ton@email.com" />
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>

                    <div class="auth-field">
                        <label asp-for="Input.Password">Mot de passe</label>
                        <input asp-for="Input.Password" class="form-control" autocomplete="new-password" aria-required="true" placeholder="••••••••" />
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>

                    <div class="auth-field">
                        <label asp-for="Input.ConfirmPassword">Confirme le mot de passe</label>
                        <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" aria-required="true" placeholder="••••••••" />
                        <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                    </div>

                    <button id="registerSubmit" type="submit" class="btn btn-gold w-100 btn-lg">S'inscrire</button>
                </form>
            </div>
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js"></script>
</body>
</html>